# ApproveOps

Generated by [saas-core](https://github.com/code-shame/saas-core) factory on 2026-05-24T19:42:41.400Z.

**Preset:** `agentsec-mvp`

**Modules:**

- `auth.clerk`
- `db.postgres.prisma`
- `ai.core`
- `ai.output-wrapper`
- `security.analytics`
- `security.prompt-injection`
- `mcp.rest`

## Setup

```bash
cp .env.example .env.local
# Fill in .env.local with your credentials

npm install
npm run db:generate
npm run db:push
npm run dev
```

## Project structure

```
src/
  app/
    (dashboard)/   # Protected product pages
    (auth)/        # Auth flows
    api/           # API routes
  lib/             # Business logic and integrations
  components/      # UI components
prisma/
  schema.prisma    # Database schema
.saas-core/
  product-manifest.json  # Factory-generated metadata
```

## Product manifest

See [`.saas-core/product-manifest.json`](.saas-core/product-manifest.json) for the full
module list and factory parameters used to generate this scaffold.
